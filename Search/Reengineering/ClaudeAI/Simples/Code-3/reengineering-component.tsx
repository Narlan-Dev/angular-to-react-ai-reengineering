import React from 'react';
import ChartModel from '../../models/Chart.model';

interface KpiProps {
  chart: ChartModel;
}

const Kpi: React.FC<KpiProps> = ({ chart }) => {
  return null;
};

export default Kpi;
